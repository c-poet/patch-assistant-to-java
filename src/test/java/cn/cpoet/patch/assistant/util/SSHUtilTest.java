package cn.cpoet.patch.assistant.util;

import com.jcraft.jsch.Session;
import org.junit.jupiter.api.Test;

/**
 * @author CPoet
 */
class SSHUtilTest {

    @Test
    void execCommand() {
        // Session session = SSHUtil.createSession();
        // SSHUtil.uploadFiles(session, "/root/docker", new String[]{
        //         "Dockerfile",
        //         appTree.getRootNode().getName()
        // }, "demo".getBytes(), bytes);
        // int status = SSHUtil.execCmd(session, "docker build -t demo:1.0.0 /root/docker");
        // System.out.println(status);
        // status = SSHUtil.execCmd(session, "docker save -o /root/docker/demo.tar demo:1.0.0");
        // System.out.println(status);
        // SSHUtil.closeSession(session);
    }
}